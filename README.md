This module has 3 goals for the Bandoo project:
1. set the logo
2. provide a quick way to define the list of modules to install
3. fix a bug that apparently exists in the OCA 18 HR module
   (but which I hadn't had time to investigate further)
   See: `controllers/main.py` class `HrAttendance`
